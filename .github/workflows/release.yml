name: Build and Package

on:
  push:
    tags:
      - 'v*'
    branches:
      - main
    paths-ignore:
      - 'README.md'
      - 'LICENSE'
      - '.github/ISSUE_TEMPLATE/**'
  pull_request:
    branches:
      - main
    paths-ignore:
      - 'README.md'
      - 'LICENSE'
  workflow_dispatch:

permissions:
  contents: write
  packages: read

jobs:
  build-desktop:
    name: Build Desktop (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            platform: mac
            script: dist:mac
          - os: windows-latest
            platform: win
            script: dist:win
          - os: ubuntu-latest
            platform: linux
            script: dist:linux
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Setup macOS build env
        if: matrix.os == 'macos-latest'
        run: |
          echo "MACOSX_DEPLOYMENT_TARGET=11.0" >> $GITHUB_ENV
          echo "CSC_IDENTITY_AUTO_DISCOVERY=false" >> $GITHUB_ENV
          echo "CSC_FOR_PULL_REQUEST=false" >> $GITHUB_ENV

      - name: Setup Linux dependencies
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          # 安装所有依赖，单个包缺失不阻断整体
          for pkg in \
            libnss3 libatk1.0-0 libatk-bridge2.0-0 libcups2 libdrm2 \
            libxkbcommon0 libxcomposite1 libxdamage1 libxfixes3 libxrandr2 \
            libgbm1 libpango-1.0-0 libcairo2 rpm squashfs-tools \
            fakeroot dpkg-dev dpkg \
            libasound2t64 libasound2 libfuse2t64 libfuse2 fuse; do
            echo "::: installing $pkg ..."
            sudo apt-get install -y "$pkg" || echo "::: skip $pkg (not found)"
          done
          # 验证关键依赖
          echo "::: verify fakeroot: $(which fakeroot || echo MISSING)"
          echo "::: verify dpkg-deb: $(which dpkg-deb || echo MISSING)"

      # 根据当前 ref 设置 package.json 版本号，让产物版本与 tag 一致
      #   tag v0.0.6 -> 版本 0.0.6；推 main -> nightly.<sha前7位>
      - name: Set package version
        shell: bash
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/v* ]]; then
            VER="${GITHUB_REF_NAME#v}"
          else
            VER="0.0.0-nightly.${GITHUB_SHA:0:7}"
          fi
          export VER
          node -e "const fs=require('fs');const j=JSON.parse(fs.readFileSync('package.json'));j.version=process.env.VER;fs.writeFileSync('package.json',JSON.stringify(j,null,2));"
          echo "package.json version => $(node -p "require('./package.json').version")"

      - name: Install dependencies
        run: npm install --ignore-scripts

      - name: Build TypeScript
        run: npm run build

      - name: Package (${{ matrix.platform }})
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CSC_IDENTITY_AUTO_DISCOVERY: false
          CSC_FOR_PULL_REQUEST: false
        run: npm run ${{ matrix.script }}

      - name: Upload macOS artifacts
        if: matrix.os == 'macos-latest'
        uses: actions/upload-artifact@v4
        with:
          name: macos-artifacts
          path: |
            release/*.dmg
            release/*.zip
          if-no-files-found: warn

      - name: Upload Windows artifacts
        if: matrix.os == 'windows-latest'
        uses: actions/upload-artifact@v4
        with:
          name: windows-artifacts
          path: |
            release/*.exe
          if-no-files-found: warn

      - name: Upload Linux artifacts
        if: matrix.os == 'ubuntu-latest'
        uses: actions/upload-artifact@v4
        with:
          name: linux-artifacts
          path: |
            release/*.AppImage
            release/*.deb
          if-no-files-found: warn

  build-android:
    name: Build Android APK
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Setup Java 21
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          # Capacitor 8 的 @capacitor/android 需 Java 21（source release 21），17 会报 invalid source release
          java-version: '21'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3
        with:
          cmdline-tools-version: 'latest'
          # compileSdk=36（见 android/variables.gradle），必须安装 android-36 + 对应 build-tools
          packages: 'platform-tools platforms;android-36 build-tools;36.0.0'
          accept-android-sdk-licenses: true

      - name: Install dependencies
        run: npm install --ignore-scripts

      - name: Prepare Capacitor Android project
        shell: bash
        run: |
          set -euo pipefail
          
          # 查找 Android SDK 路径
          SDK_PATH="${ANDROID_SDK_ROOT:-${ANDROID_HOME:-}}"
          if [ -z "$SDK_PATH" ] || [ ! -d "$SDK_PATH" ]; then
            # 尝试从 setup-android action 的常见安装路径查找
            for p in "$HOME/.android/sdk" "/usr/local/lib/android/sdk" "/opt/android/sdk" "$ANDROID_HOME" "$ANDROID_SDK_ROOT"; do
              [ -d "$p" ] && { SDK_PATH="$p"; break; } || true
            done
          fi
          # 如果还是没找到，全局搜索 sdkmanager
          if [ -z "$SDK_PATH" ] || [ ! -d "$SDK_PATH" ]; then
            SDK_PATH=$(dirname "$(dirname "$(find / -name 'sdkmanager' -type f 2>/dev/null | head -1)")" 2>/dev/null || echo "")
            [ -z "$SDK_PATH" ] && SDK_PATH="/usr/local/lib/android/sdk"  # 兜底
          fi
          echo "SDK_PATH=${SDK_PATH}"
          echo "ANDROID_HOME=${SDK_PATH}" >> $GITHUB_ENV
          echo "ANDROID_SDK_ROOT=${SDK_PATH}" >> $GITHUB_ENV
          export ANDROID_HOME="$SDK_PATH"
          export ANDROID_SDK_ROOT="$SDK_PATH"

          # 准备移动端 web 资源
          mkdir -p capacitor/web
          cp src/mobile/index.html capacitor/web/

          # android/ 原生工程已提交到仓库，只需同步 web 资源（Capacitor 会在 android 目录存在时更新 assets/plugins）
          npx cap sync android 2>&1

          # 写入 local.properties 确保 Gradle 能找到 SDK
          echo "sdk.dir=${SDK_PATH}" > android/local.properties
          cat android/local.properties

          # 确保 gradlew 可执行（已随 android/ 提交）
          chmod +x android/gradlew || { echo "FATAL: android/gradlew 缺失"; exit 1; }
          echo "::: Android 工程准备完成"

      - name: Assemble Android Debug APK
        shell: bash
        run: |
          set -euo pipefail
          # 从环境变量或 local.properties 获取 SDK 路径
          SDK_PATH="${ANDROID_HOME:-${ANDROID_SDK_ROOT:-}}"
          echo "ANDROID_HOME=${SDK_PATH}"
          export ANDROID_HOME="$SDK_PATH"
          export ANDROID_SDK_ROOT="$SDK_PATH"

          if [ ! -f android/gradlew ]; then
            echo "FATAL: android/gradlew 不存在"
            exit 1
          fi

          cd android
          chmod +x gradlew
          
          # 确保 local.properties 存在
          [ -f local.properties ] || echo "sdk.dir=${SDK_PATH}" > local.properties
          
          echo "::: Gradle 版本:"
          ./gradlew --version
          echo "::: 开始构建 APK..."
          ./gradlew :app:assembleDebug --no-daemon -x lint 2>&1
          cd ..
          
          echo "::: APK 产物列表:"
          find android/app/build/outputs -type f \( -name "*.apk" -o -name "*.aab" \) -exec ls -la {} \; 2>/dev/null || echo "未找到 APK"
          
          # 如果没有 APK，报错
          APK_COUNT=$(find android/app/build/outputs -type f -name "*.apk" 2>/dev/null | wc -l)
          if [ "$APK_COUNT" -eq 0 ]; then
            echo "FATAL: 没有生成任何 APK 文件"
            exit 1
          fi

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: android-artifacts
          path: |
            android/app/build/outputs/apk/**/*.apk
            android/app/build/outputs/bundle/**/*.aab
          if-no-files-found: warn

  release:
    name: Publish GitHub Release
    needs: [build-desktop, build-android]
    # 推版本 tag（v*）或推 main 都生成 release（main 为滚动 latest，tag 为正式版本）
    if: startsWith(github.ref, 'refs/tags/v') || github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts/

      - name: Prepare release asset folder
        run: |
          mkdir -p release-artifacts
          find artifacts -type f -not -name "*.log" | xargs -I{} cp -f {} release-artifacts/ 2>/dev/null || true
          ls -la release-artifacts/

      - name: Determine release tag
        id: ver
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            echo "tag=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"
            echo "name=Release ${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"
            echo "prerelease=false" >> "$GITHUB_OUTPUT"
          else
            echo "tag=latest" >> "$GITHUB_OUTPUT"
            echo "name=Latest (main 自动构建)" >> "$GITHUB_OUTPUT"
            echo "prerelease=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Create GitHub Release and upload assets
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.ver.outputs.tag }}
          name: ${{ steps.ver.outputs.name }}
          generate_release_notes: true
          draft: false
          prerelease: false
          files: release-artifacts/*
